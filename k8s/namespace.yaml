apiVersion: v1
kind: Namespace
metadata:
  name: pedidos-veloz
  labels:
    app.kubernetes.io/part-of: pedidos-veloz
    pod-security.kubernetes.io/enforce: baseline
    pod-security.kubernetes.io/audit: restricted
    pod-security.kubernetes.io/warn: restricted
---
# Justificativa PSA: enforce=baseline permite o MVP sem privilegiar containers;
# audit/warn=restricted antecipa hardening (runAsNonRoot, drop capabilities).
